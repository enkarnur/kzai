name: kazakh_chat
description: 面向哈萨克用户的极简聊天应用（阿拉伯哈萨克文输入 / DeepSeek 问答 / 本地转写）
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  shared_preferences: ^2.2.2
  speech_to_text: ^6.6.0
  permission_handler: ^11.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
